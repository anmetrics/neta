import type { HttpMethod, RetryOptions } from './types.js';

export declare const HTTP_METHODS: HttpMethod[];
export declare const DEFAULT_RETRY: RetryOptions;
export declare const DEFAULT_TIMEOUT: number;
